MODULE(tests)
MODULE(pe)
MODULE(elf)
MODULE(macho)
MODULE(math)

#ifdef DOTNET_MODULE
MODULE(dotnet)
#endif

#ifdef CUCKOO_MODULE
MODULE(cuckoo)
#endif

#ifdef MAGIC_MODULE
MODULE(magic)
#endif

#ifdef HASH_MODULE
MODULE(hash)
#endif
