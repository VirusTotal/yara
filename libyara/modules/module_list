MODULE(tests)
MODULE(pe)
MODULE(elf)
MODULE(math)

#ifdef DOTNET_MODULE
MODULE(dotnet)
#endif

#ifdef CUCKOO_MODULE
MODULE(cuckoo)
MODULE(json)
#endif

#ifdef MAGIC_MODULE
MODULE(magic)
#endif

#ifdef HASH_MODULE
MODULE(hash)
#endif
